name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Generate root index.html
        run: npm run generate-root-index
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Move niche folders to Pages root
        run: |
          # If sites/ directory exists, move all subdirectories to root
          if [ -d "sites" ]; then
            echo "📦 Moving niche folders from sites/ to root..."
            shopt -s dotglob
            for dir in sites/*; do
              if [ -d "$dir" ]; then
                basename=$(basename "$dir")
                echo "  Moving $dir to ./$basename/"
                mv "$dir" .
              fi
            done
            # Remove empty sites directory
            rmdir sites 2>/dev/null || true
            echo "✅ Niche folders moved successfully!"
          else
            echo "ℹ️  No sites/ directory found,"
            echo "   niche folders already at root level"
          fi
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
